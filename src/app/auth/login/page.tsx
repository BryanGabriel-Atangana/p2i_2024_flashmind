import LoginForm from "@/components/auth/LoginForm";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="">
      <LoginForm />
    </div>
  );
};

export default page;
